{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run test",
            "type": "chrome",
            "webRoot": "${workspaceFolder}/test",
            "request": "launch",
            "url": "http://localhost:3444"
        }
    ]
}
